/*
 * This file was generated by {{=it.packager.name}} v{{=it.packager.version}}.
 * {{=it.packager.url}}
 *
 * Library: {{=it.library.name}}
 * Conan.io recipe reference: {{=it.library.reference}}
 * Conan.io remote: {{=it.conan.remote}}
 * Conan.io package id: {{=it.conan.packageId}}
 */

const { loadLibrary } = require("../loader");

const input = require("./index.json");

module.exports = {
  // NOTE: named wrapper function with library name prefix to make it easier to spot in a stack trace.
  // NOTE: assumes that the library name is in a suitable format.
  load: async function {{=it.library.name}}LibraryLoader() {
    return loadLibrary(__dirname, input);
  },
};
